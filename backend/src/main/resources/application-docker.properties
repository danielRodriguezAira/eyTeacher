spring.datasource.url=jdbc:mysql://mysql:3306/eyteacherdb?connectTimeout=5000&socketTimeout=10000&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.flyway.url=jdbc:mysql://mysql:3306/eyteacherdb?connectTimeout=5000&socketTimeout=10000&allowPublicKeyRetrieval=true&useSSL=false
spring.flyway.user=${MYSQL_USER}
spring.flyway.password=${MYSQL_PASSWORD}

server.port=8080
server.ssl.enabled=false