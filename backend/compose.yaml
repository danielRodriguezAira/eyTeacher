services:
  eyteacher-backend-app:
    build: .
    restart: on-failure:5
    ports:
      - "8080:8080"
    profiles:
      - production
    labels:
      - "org.springframework.boot.ignore=true"
    networks:
      - default
      - eyteacher-network
    environment:
      SPRING_PROFILES_ACTIVE: docker
      JWT_SECRET: ${JWT_SECRET:-eyteacher-super-secret-key-512bits-long-enough-for-hs512-algorithm-validation}
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/eyteacherdb?connectTimeout=10000&socketTimeout=30000&allowPublicKeyRetrieval=true&useSSL=false
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-eyteacher}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-eyteacher}
      SPRING_FLYWAY_URL: jdbc:mysql://mysql:3306/eyteacherdb?connectTimeout=10000&socketTimeout=30000&allowPublicKeyRetrieval=true&useSSL=false
      SPRING_FLYWAY_USER: ${MYSQL_USER:-eyteacher}
      SPRING_FLYWAY_PASSWORD: ${MYSQL_PASSWORD:-eyteacher}
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-eyteacher}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-eyteacher}
    depends_on:
      mysql:
        condition: service_healthy
      rabbitmq:
        condition: service_started

  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: eyteacherdb
      MYSQL_USER: ${MYSQL_USER:-eyteacher}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-eyteacher}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-eyteacher}
    ports:
      - "3306:3306"
    networks:
      - default
      - eyteacher-network
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u$${MYSQL_USER} -p$${MYSQL_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  rabbitmq:
    image: rabbitmq:3-management
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-eyteacher}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-eyteacher}
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - default
      - eyteacher-network

networks:
  eyteacher-network:
    external: true