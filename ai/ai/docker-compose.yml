version: "3.8"

services:
  eyteacher-ai-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    container_name: eyteacher-ai-app
    network_mode: host
    environment:
      - SERVER_PORT=8081
      - SPRING_AI_OLLAMA_BASE_URL=http://localhost:11434
